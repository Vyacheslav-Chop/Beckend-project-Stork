description: Unauthorized error
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - title
        - data
      properties:
        status:
          type: integer
          example: 401
        title:
          type: string
          example: UnauthorizedError
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Unauthorized access'
