description: Conflict
content:
  application/json:
    schema:
      type: object
      required:
        - status
        - title
        - data
      properties:
        status:
          type: integer
          example: 409
        title:
          type: string
          example: Сonflict
        data:
          type: object
          required:
            - message
          properties:
            message:
              type: string
              example: 'Validation failed or invalid request data'
